/*$(document).ready(function() {
  // Strech up the input size
  $('.nav-search-form form input').on('focus', function() {
    if ($(window).width() + 15 <= 571) {
      // if screen size is xxs (571px or less), make the input bigger
      $(this).css('width', '550%');
    }
    else {
      $(this).css('width', '250%');
    }
    $(this).attr('placeholder', 'Pelo que você procura?');
  });

  // Strech down the input size
  $('.nav-search-form form input').on('blur', function() {
    $(this).css('width', '100%');
    $(this).attr('placeholder', '');
  });
})
*/
