<%= simple_form_for([@make, @image]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :public %>

  <%= f.input :image_url %>
  <%= f.input :image_url_cache, as: :hidden %>
  <%= f.submit %>
<% end %>
