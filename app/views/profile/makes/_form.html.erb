<%= simple_form_for(@make) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :make_types,  collection: @types, label: 'Tipo da Make' %>
  <%= f.input :public %>

  <%= f.submit %>
<% end %>
